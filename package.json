{
  "name": "lume",
  "version": "1.0.0",
  "private": true,
  "description": "Lume — a global daily-life super-app. Static, dependency-free at runtime.",
  "scripts": {
    "test": "node tests/architecture.js && node tests/verify.js && node tests/interact.js && node tests/controls.js && node tests/regress.js && node tests/notify.js && node tests/account.js && node tests/auth.js",
    "serve": "node tests/serve.js"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
